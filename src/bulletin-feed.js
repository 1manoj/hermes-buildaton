export function shortText(text,limit=160){const value=String(text||"").trim();if(value.length<=limit)return value;return `${value.slice(0,limit).replace(/\s+\S*$/,"").trim()}…`;}
export function buildTopStories(rundown=[]){return [...rundown].sort((a,b)=>(a.rank||99)-(b.rank||99)).slice(0,5).map((story,index)=>({title:story.title,summary:story.summary||"",shortSummary:shortText(story.summary),rank:story.rank||index+1,url:story.url,source:story.source,category:story.category||"general"}));}
