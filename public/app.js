const names=["Monitor","Editor","Writer","Judge","Anchor","Publisher"];
document.querySelector("#agents").innerHTML=names.map((n,i)=>`<div class="agent"><span class="num">0${i+1}</span><strong>${n}</strong></div>`).join("");
const run=document.querySelector("#run"),trace=document.querySelector("#trace"),status=document.querySelector("#status");
run.onclick=async()=>{run.disabled=true;status.textContent="Agents working…";trace.innerHTML="<p>Starting autonomous newsroom…</p>";try{const r=await fetch("/api/run",{method:"POST"});const data=await r.json();trace.innerHTML=data.steps.map(s=>`<div class="step"><strong>${s.agent}</strong><span class="${s.status}">${s.status==='complete'?'✓ Completed handoff':s.error}</span><span>${s.latencyMs}ms</span></div>`).join("");status.textContent=data.status.toUpperCase();if(data.publication){document.querySelector("#headline").textContent=data.publication.headline;document.querySelector("#bulletin").textContent=data.publication.text;}}catch(e){status.textContent="ERROR";trace.textContent=e.message}finally{run.disabled=false}};
