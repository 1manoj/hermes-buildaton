import { config } from "../src/config.js";
import { convexDueWatchRules,convexWatchOwner,convexAlertExists,convexSaveWatchAlert,convexMarkWatchChecked } from "../src/convex.js";
import { searchLinkupPlan } from "../src/integrations.js";
import { watchQuery,eventFingerprint,estimateMateriality,shouldAlert } from "../src/watch-rules.js";
const due=await convexDueWatchRules(Date.now());let checked=0,sent=0,skipped=0,errors=0;
for(const rule of due){const now=Date.now();try{const user=await convexWatchOwner(rule.userId),query=watchQuery(rule);const stories=await searchLinkupPlan(config.linkupKey,{queries:[{query}],domains:[]});for(const event of stories.slice(0,8)){const fingerprint=eventFingerprint(event);if(await convexAlertExists(rule._id,fingerprint)){skipped++;continue;}const materiality=estimateMateriality(event,rule);if(!shouldAlert(rule,{materiality})){skipped++;continue;}let status="pending",telegramMessageId;if(user?.telegramChatId&&config.telegramToken){const urgency=rule.urgency.toUpperCase(),text=`🔔 ${urgency} INTELLIGENCE ALERT\n\n${rule.targetType}: ${rule.target}\n\n${event.title}\n\n${event.summary}\n\nMateriality: ${materiality}/100\nSource: ${event.source}\n${event.url}\n\nTracked events: ${rule.eventTypes.join(", ")}`;const response=await fetch(`https://api.telegram.org/bot${config.telegramToken}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:user.telegramChatId,text:text.slice(0,4096),disable_web_page_preview:true})}),result=await response.json();status=result.ok?"sent":"failed";telegramMessageId=result.result?.message_id?String(result.result.message_id):undefined;if(result.ok)sent++;}else status="awaiting-telegram";await convexSaveWatchAlert({ruleId:rule._id,userId:rule.userId,fingerprint,title:event.title,summary:event.summary,url:event.url,source:event.source,materiality,status,telegramMessageId});}checked++;}catch(e){errors++;console.error(`rule ${rule._id}: ${e.message}`);}finally{await convexMarkWatchChecked(rule._id,rule.urgency,now).catch(()=>{});}}
console.log(JSON.stringify({due:due.length,checked,sent,skipped,errors,at:new Date().toISOString()}));
if(errors)process.exitCode=1;
